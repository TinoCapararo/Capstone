# Docker usage information

## Prerequisities
- Have Docker Desktop installed: https://docs.docker.com/desktop/install

## Install containers
- Run `docker-compose -p capstone -f docker-compose-dev.yml up`
This command installs **mariadb** and **phpmyadmin** (on port 8080)

## Start containers
- Go to Docker Desktop and click play button on capstone
